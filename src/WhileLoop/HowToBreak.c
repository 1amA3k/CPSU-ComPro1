#include <stdio.h>

int main() {
    int N, cnt = 0;

    while (1) {
        scanf("%d", &n);
        if (n==0) {
            break;
        } else {
            cnt++;
        }
    }
    printf("%d", cnt);
    
    return 0;
}